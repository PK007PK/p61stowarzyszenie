import styled from 'styled-components';

export const CooperationStyle = styled.div`
    padding: var(--spacingMedium) 0;
    .container {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-around;
    }
    .cityName {
        text-align: center;
    }
    .image {
        width: 200px;
    }
`;
