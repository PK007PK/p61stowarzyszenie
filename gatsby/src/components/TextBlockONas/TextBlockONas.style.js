import styled from 'styled-components';

export const TextBlockONasStyle = styled.div`
    margin-bottom: 40px;
    .title {
        margin-bottom: 0px;
    }

    .tagBar {
        display: flex;
        margin-bottom: 20px;
    }

    .tagContainer {
        margin-right: 20px;
        display: flex;
        align-items: center;
        font-weight: 300;
        font-size: 18px;
    }

    .hash {
        color: var(--colorActiveSecondary);
    }

    .bottomButtonsBar {
        display: flex;
    }

    .bottomBtn {
        color: white;
    }

    .pdfIcon {
        height: 25px;
    }
`;
