import styled from 'styled-components';

export const SectionOurProjectsVerticalStyles = styled.div`
    .title {
        text-align: center;
        margin-bottom: var(--spacingRegular);
    }
    .item {
        margin-bottom: var(--spacingRegular);
    }
`;
